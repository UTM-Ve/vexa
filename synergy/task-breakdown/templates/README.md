# templates — files a user drops into their workspace

[`tasks.md`](tasks.md) is the governed config for this stage: copy it to `agents/tasks.md` in a
workspace to steer what counts as a task there. Absent, the stage runs on its built-in defaults.
